node_modules/
dist/
*.tsbuildinfo
.turbo/
.pnpm-store/
emulate.config.yaml
emulate.config.yml
.env*
!.env.example
# never commit npm auth — CI uses the NPM_TOKEN secret, local uses ~/.npmrc
.npmrc
.DS_Store

# emulate CLI README is copied from root by prepack
packages/emulate/README.md

# opensrc - source code for packages
opensrc/
.DS_Store
